/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          "themegrilldemos.com",
          "img.freepik.com",
          "images.pexels.com"
        ],
      },
}

module.exports = nextConfig
