/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          "themegrilldemos.com",
          "images.pexels.com",
          "img.freepik.com"
        ],
      },
}

module.exports = nextConfig
